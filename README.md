# Happy Homebody Soda

Static bilingual onboarding guide for authorized testers.

## Local preview

Open `index.html` in a browser. No build step or external dependency is required.

## Publish with GitHub Pages

In the GitHub repository, open **Settings > Pages**, choose **Deploy from a branch**, then select the `main` branch and `/ (root)` folder.
